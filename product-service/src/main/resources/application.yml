server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/userdb
    username: useradmin
    password: useradmin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true